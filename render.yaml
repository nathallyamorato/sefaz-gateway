services:
  - type: web
    name: sefaz-gateway
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: GATEWAY_SECRET
        sync: false
